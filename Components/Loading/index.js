import React from "react";
import { Spinner, Container } from "native-base";

const Loading = () => {
  return (
    <Container>
      <Spinner color="blue" />
    </Container>
  );
};

export default Loading;
